Back end server module, can run the following to start it up:

### `npm install`

### `npm start`

The backend server will run on [http://localhost:5000](http:localhost:5000)

To run test for backend server:

### `npm test`